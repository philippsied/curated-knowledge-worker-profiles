# Security

- NEVER delete or rewrite working code without explicit permission.
- Irreversible actions include explicitly: commit, push, deploy, force-push,
  reset --hard, rm -rf, drop, disable hooks.
